npm init
npm i react react-dom --save-dev
npm i babel-loader babel-core babel-preset-env babel-preset-react --save-dev
npm install --save react-router-dom
npm install lodash --save
Don’t forget to configure Babel! Create a new file named .babelrcinside the project folder:

{
  "presets": ["env", "react"]
}


webpack.config.js
	const path = require("path");
	module.exports = {
	  entry: ["./src/js/app.js"],
	  output: {
	    path: path.resolve(__dirname, "dist"),
	    filename: "js/[name].js"
	  },
	  module: {
	    rules: [
	      {
		test: /\.js$/,
		exclude: /node_modules/,
		use: {
		  loader: "babel-loader"
		}
	      }
	    ]
	  }
	};


It takes ./src/js/app.jsas the entry point to produce an ouput into ./dist/js/main.js

Sublime
	https://tutorialzine.com/2016/10/15-awesome-sublime-text-plugins-for-web-development
	- Ctrl + shift + P -  Package Control. Then "Package control" gets install
	- Run “Package Control: Install Package” command, find and install Emmet plugin. 
	
	

